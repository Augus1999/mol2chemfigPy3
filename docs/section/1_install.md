## Install from PyPI

```bash
$ pip install -U mol2chemfigPy3
```

## Install from source

```bash
$ pip install git+https://github.com/Augus1999/mol2chemfigPy3.git
```

> ⚠️ Note that you need to have `setuptools` installed.
